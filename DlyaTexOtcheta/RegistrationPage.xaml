<Window x:Class="DlyaTexOtcheta.RegistrationPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DlyaTexOtcheta"
        mc:Ignorable="d"
            Title="Вход в систему" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Content="Вход" Grid.Row="0" HorizontalAlignment="Center" FontStyle="Italic"/>
        <TextBlock Text="Логин" Grid.Row="1" HorizontalAlignment="Center"/>
        <TextBox x:Name="UsernameBox" Grid.Row="2" Margin="0,5,0,10" Height="30" />
        <TextBlock Text="Пароль" Grid.Row="3" HorizontalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" Margin="0,5,0,10" Height="30" />

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center" Height="50">
            <Button Content="Войти" Width="100" Click="LoginButton_Click" Margin="5" />
            <Button Content="Регистрация" Width="100" x:Name="RegistrationButton" Click="RegistrationButton_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
